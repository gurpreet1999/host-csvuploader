<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<!-- bootstrap link -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
	<!-- Local css -->
	<link rel="stylesheet" type="text/css" href="/css/table.css">
	<!-- Font-Awesome Link -->
	<script src="https://kit.fontawesome.com/1dd5c5cd8c.js" crossorigin="anonymous"></script>
	<title>CSV Reader | Tabular</title>
</head>
<body>
	<main>
		<header>
            <div id="home-button">
                <a href="/v1/">Home</a>
            </div>
            <form id="searchForm" action="/v1/file/search" method="post">
                <div class="input-group">
                   <input type="text" id="searchItem" class="form-control" placeholder="Search data..." name="search"  value="" >
                   <button id="go" class="btn btn-default" type="submit" >
                      Go
                   </button>
                </div>
             </form>
			 <div>
				<p id="total-rows">Total Rows: <%= csvData.length%></p>
			 </div>
        </header>
		<div id='table-container'>
			<table style="width:100%">
				<tr>
					<% for (const [key, value] of Object.entries(csvData[0])) { %>
					<th ><a href="/v1/file/sort/<%= data %>/<%= key %>"  ><%= key %></a></th>
					<% } %>
				</tr>


				
				<% for(let i=0;i<csvData.length;i++) { %>
				<tr id="row-<%= i %>">
					<% for(const [key, value] of Object.entries(csvData[i])) { %>
					    <td class="col-md-2"><%= value %></td>
				    <% } %>
				</tr>
				<% } %>
			
			</table>
		</div>
	</main>

  	<div class="pagination">
		<% for(let i=0;i<lengthOfData;i++) { %>
             <a href="/v1/file/showdata/<%= i+1 %>"> <%= i+1 %></a>
		
			<% } %>
	</div>


<script>

console.log(csvData)

</script>

	<!-- bootstrap bundle script CDN -->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
	<script type="text/javascript" src="/js/table.js"></script>
</body>
</html>